version(1);

project_name = "CPU 6502 Emulator";

patterns = {
	"*.cpp",
	"*.c",
	"*.cc"
	"*.h",
	"*.bat",
	"*.sh",
	"*.4coder",
	"Makefile",
	".txt",
	".*",
};

load_paths = {
	{
		{
			{ ".", .relative = true, .recursive = false },
			{ "code/", .relative = true, .recursive = true },
			{ "test/", .relative = true, .recursive = true },
			{ "misc/", .relative = true, .recursive = true }
		}, 
		.os="win"
	},
};